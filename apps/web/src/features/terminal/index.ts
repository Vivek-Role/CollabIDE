/**
 * The terminal feature's public surface — same barrel rule as every other
 * feature. Run output arrives over SSE, never over the collaboration socket.
 */
export { RunPanel } from './RunPanel';
