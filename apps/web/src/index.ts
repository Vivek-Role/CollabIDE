/**
 * Web entrypoint — stub.
 *
 * Vite, React, Tailwind and the dark IDE shell arrive in module 2.1, which
 * also switches this workspace to bundler module resolution. Until then this
 * is a plain TS file so the workspace participates in the root build.
 */

export const WEB_NAME = '@collab/web';
